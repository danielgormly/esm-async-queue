{
  "name": "esm-async-queue",
  "version": "0.0.1",
  "description": "A single class `Queue` to create an immediately starting queue of asynchronous or synchronous tasks, processing *n* tasks concurrently. Dependency free.",
  "main": "index.js",
  "scripts": {
    "start": "node --loader ts-node/esm ./src/index.ts",
    "build": "tsc",
    "test": "node --loader ts-node/esm ./src/index.spec.ts"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tape": "^5.1.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
